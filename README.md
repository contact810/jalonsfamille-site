# Site vitrine Jalons Famille

Site statique (HTML/CSS/JS) déployé sur Netlify.

- `index.html` : landing
- `mentions-legales.html`, `confidentialite.html`, `contact.html` : pages légales
- `css/`, `js/`, `images/` : assets

Le formulaire de contact utilise Netlify Forms (fonctionne une fois déployé).
